{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "main": "src/app.js",
  "scripts": {
    "test": "jest --runInBand --passWithNoTests",
    "test:unit": "jest --testPathPattern=tests/unit --runInBand --passWithNoTests",
    "test:integration": "jest --testPathPattern=tests/integration --runInBand",
    "setup-test-db": "node scripts/setup-test-db.js",
    "coverage": "jest --coverage"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.3",
    "mongodb-memory-server": "^8.12.0",
    "jest-environment-node": "^29.0.0"
  }
}